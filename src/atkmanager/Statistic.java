package atkmanager;

import java.text.SimpleDateFormat;
import java.util.ArrayList;

/**
 *
 * @author adinb
 */
public class Statistic {
    ArrayList <Integer> numbers;
    
    //String sql = "SELECT SUM(jumlah) FROM pesananATK WHERE strftime('%Y-%m', tanggal_pesanan) = '" + year + "-" + month + "';";
    
}
